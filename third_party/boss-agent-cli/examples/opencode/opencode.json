{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "boss-agent": {
      "type": "local",
      "command": [
        "uv",
        "run",
        "boss-mcp",
        "--data-dir",
        "./.boss-agent"
      ],
      "enabled": true,
      "timeout": 10000
    }
  }
}
